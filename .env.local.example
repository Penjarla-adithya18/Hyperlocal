# ─── Supabase ────────────────────────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Service role key — used server-side to call edge functions securely (email, etc.)
# Never expose this to the browser (no NEXT_PUBLIC_ prefix).
# Find it in: Supabase Dashboard → Project Settings → API → service_role key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ─── App URL (used in email templates) ───────────────────────────────────────
NEXT_PUBLIC_APP_URL=https://yourdomain.in

# ─── SMTP Email ───────────────────────────────────────────────────────────────
# SMTP credentials are stored as Supabase secrets, NOT in this file.
# The email edge function (supabase/functions/email/index.ts) reads them
# from Deno.env automatically in production.
#
# Set them once via the Supabase CLI:
#   supabase secrets set SMTP_HOST=smtp.gmail.com
#   supabase secrets set SMTP_PORT=587
#   supabase secrets set SMTP_USER=yourname@gmail.com
#   supabase secrets set SMTP_PASS=your-app-password-here
#   supabase secrets set EMAIL_FROM_NAME="HyperLocal Jobs"
#
# Gmail: Enable 2FA → generate an App Password at
#   https://myaccount.google.com/apppasswords
#
# SendGrid: Use smtp.sendgrid.net, port 587, user=apikey, pass=<your_api_key>

# ─── Twilio Verify (SMS OTP) ──────────────────────────────────────────────────
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your-auth-token
TWILIO_VERIFY_SERVICE_SID=VAxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OTP_DEFAULT_COUNTRY_CODE=+91

# ─── WATI WhatsApp (set as Supabase secrets, not here) ───────────────────────
# supabase secrets set WATI_API_URL=https://live-mt-server.wati.io/XXXXX
# supabase secrets set WATI_API_KEY=your-bearer-token
#
# These are read inside the Supabase Edge Function supabase/functions/wati/index.ts
# and must NOT go in .env.local (they are server-side Deno secrets only).

# ─── Google Maps (for worker/jobs location search) ───────────────────────────
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# ─── Gemini AI (for RAG resume search) ───────────────────────────────────────
GEMINI_API_KEY=your-gemini-api-key

# ─── PAN KYC (Sandbox API) ───────────────────────────────────────────────────
SANDBOX_API_KEY=your-sandbox-api-key
