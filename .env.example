# ── Supabase project config (required) ──────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://<your-project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<your-anon-key>

# ── Edge Functions ───────────────────────────────────────────────────────────
# SUPABASE_SERVICE_ROLE_KEY is injected automatically into edge functions by
# Supabase – you do NOT need to set it here for production.
# For local development with `supabase start`, set it if running edge functions
# locally:
# SUPABASE_SERVICE_ROLE_KEY=<your-service-role-key>

# ── WATI WhatsApp Business API ───────────────────────────────────────────────
# Sign up at https://app.wati.io → Settings → API → Generate Token
# Set as Supabase secrets:  npx supabase secrets set WATI_API_URL=... WATI_API_KEY=...
WATI_API_URL=https://live-mt-server.wati.io/<your-wati-account-id>
WATI_API_KEY=<your-wati-bearer-token>

# ── Gemini AI (round-robin key pool) ──────────────────────────────────────────
# Comma-separated list of Gemini API keys (no spaces).
# Get keys at: https://aistudio.google.com/app/apikey
# For local dev: set in .env.local
# For Supabase edge functions: npx supabase secrets set GEMINI_API_KEYS=key1,key2,...
NEXT_PUBLIC_GEMINI_API_KEYS=<key1>,<key2>,<key3>
